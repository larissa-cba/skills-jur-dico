# Nunca versionar documento de cliente.
# As fichas e os documentos de trabalho gerados pela skill contêm CPF, NIT,
# número de benefício, CID e diagnóstico. Ficam na pasta do cliente no Drive,
# nunca aqui.
*.docx
*.doc
*.pdf
*.png
*.jpg
*.jpeg

# Saída temporária de renderização
med/
ctps/
*_x/

# Windows
Thumbs.db
desktop.ini

# Segredos, chaves e configuração local — nunca versionar
.env
.env.*
*.pem
*.key
*.pfx
*.p12
*.crt
id_rsa
id_rsa.*
id_ed25519
id_ed25519.*
*credentials*.json
*secret*.json
*.secrets
.claude/settings.local.json
.mcp.json
